const assets = [
  { name: "??", type: "??", version: "v3", summary: "????????????" },
  { name: "???", type: "??", version: "v2", summary: "????????????" },
  { name: "?????", type: "??", version: "v1", summary: "???????????" },
];

export default function AssetsPage() {
  return (
    <main aria-labelledby="assets-title">
      <h1 id="assets-title">Asset Center ????</h1>
      <p>Asset Center ??????????????????????</p>
      <section aria-labelledby="asset-list-title">
        <h2 id="asset-list-title">????</h2>
        <ul>
          {assets.map((asset) => (
            <li key={asset.name}>
              <strong>{asset.name}</strong>?{asset.type}??? {asset.version}?
              <p>{asset.summary}</p>
            </li>
          ))}
        </ul>
      </section>
    </main>
  );
}
