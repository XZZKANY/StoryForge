{
  "name": "@storyforge/desktop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "StoryForge 桌面 IDE 应用",
  "scripts": {
    "dev": "npm run prepare:git && tauri dev",
    "build": "npm run prepare:git && npm run build:api-sidecar && tauri build",
    "build:api-sidecar": "node scripts/build-api-sidecar.mjs",
    "prepare:git": "node scripts/prepare-bundled-git.mjs",
    "verify:git-bundle": "node scripts/prepare-bundled-git.mjs --verify",
    "test:git-bundle": "node --test scripts/prepare-bundled-git.test.mjs",
    "verify": "npm run prepare:git && npm run test:git-bundle && npm run verify:git-bundle && npm --prefix frontend run typecheck && npm --prefix frontend run test && npm --prefix frontend run build && npm --prefix frontend run verify:smoke && npm --prefix frontend run verify:agent-conversation && npm run test:rust && npm run verify:tauri-smoke",
    "verify:tauri-smoke": "npm run prepare:git && node scripts/verify-tauri-smoke.mjs",
    "verify:tauri-smoke:packaged": "npm run prepare:git && node scripts/verify-tauri-smoke.mjs --release",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.3.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.1"
  }
}
