{
  "name": "@storyforge/desktop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "StoryForge 桌面 IDE 应用",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "verify": "npm --prefix frontend run typecheck && npm --prefix frontend run test && npm --prefix frontend run build && npm --prefix frontend run verify:smoke && npm run test:rust && npm run verify:tauri-smoke",
    "verify:tauri-smoke": "node scripts/verify-tauri-smoke.mjs",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.3.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.1"
  }
}
