## 项目上下文摘要（根据 AGENTS 修改计划）

生成时间：2026-05-12 14:16:49 +08:00

### 1. 相似实现分析

- **实现1**: `docs/superpowers/specs/2026-05-12-dual-mode-ai-novel-platform-design.zh-CN.md`
  - 模式：中文主规格，按产品目标、架构、资产模型、闭环、技术选型、路线图组织。
  - 可复用：保留双入口共享资产底座、模块化单体、Scene Packet、结构化评审、定向修复。
  - 需注意：缺少 AGENTS 要求的本地验证、审计文件、上下文摘要和阶段门禁。
- **实现2**: `docs/superpowers/specs/2026-05-12-dual-mode-ai-novel-platform-design.md`
  - 模式：英文镜像规格，内容与中文主规格基本一致。
  - 可复用：可作为同步检查参考。
  - 需注意：AGENTS 要求所有文档必须使用简体中文，因此英文镜像需要处理为中文说明或中文内容。
- **实现3**: `.superpowers/brainstorm/visual-1778562362/content/*.html`
  - 模式：用可视化草案表达架构层次、技术边界、Agent/RAG/评审闭环。
  - 可复用：保留“B 为核，C 为壳”和阶段路线图表达。
  - 需注意：这些是展示材料，本次不直接修改。
### 2. 项目约定

- **语言约定**：所有回复、文档、日志、审查报告必须使用简体中文；代码标识符保留项目命名。
- **文件组织**：任务审计文件写入项目本地 `.codex/`；设计规格位于 `docs/superpowers/specs/`。
- **工具顺序**：先 sequential-thinking，再 shrimp-task-manager，再直接执行。
- **验证约定**：拒绝 CI 和远程验证；所有验证必须由本地命令或可复核推理完成。

### 3. 可复用组件清单

- `AGENTS.md`：开发、文档、验证和审查准则，是本次修改的最高项目约束。
- 中文设计规格：产品与架构真相源。
- 可视化草案：用于验证计划中的架构描述没有偏离原始设计意图。

### 4. 测试策略

- 当前仓库没有实际代码和测试文件；测试策略以文档验证为主。
- 本次验证命令：检查目标文档存在、检查新增章节关键字、检查英文规格是否已转为中文说明、检查 `.codex` 审计文件存在。
- 若后续进入实现阶段，必须补充自动化单元测试、冒烟测试或功能测试。
### 5. 依赖和集成点

- **技术栈依赖**：Next.js、React、FastAPI、LangGraph、PostgreSQL、pgvector、Redis、对象存储、Langfuse、OpenTelemetry。
- **Context7 查询结果**：Next.js 使用 App Router 与根布局约定；FastAPI 支持 APIRouter 和多文件大应用组织；LangGraph 的 durable execution 依赖 checkpointer、thread_id、确定性节点和可恢复执行。
- **开源检索限制**：当前会话没有 `github.search_code` 工具；已记录限制，未将不可验证的开源实现细节写入计划结论。

### 6. 技术选型理由

- 保留模块化单体，因为当前只有设计文档，过早拆分微服务会提高验证和维护成本。
- 保留 LangGraph 作为工作流候选，因为计划要求长任务、checkpoint、人工审批和恢复。
- 保留 FastAPI 与 Next.js 组合，因为分别匹配 API 编排与复杂编辑器界面。

### 7. 关键风险点

- **边界条件**：计划过大时容易把产品愿景当实施计划，必须加入阶段门禁。
- **性能瓶颈**：检索和评审链路会带来模型成本与延迟，计划中必须保留预算、缓存和评测要求。
- **验证缺口**：当前没有代码，无法运行单元测试；本次只能做文档结构和关键约束验证。
